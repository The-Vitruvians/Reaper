#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float a, b;
    scanf("%f %f", &a, &b);
    printf("Digite um numero:\nDigite outro numero:\nSoma: %.1f", a+b);
	return 0;
}